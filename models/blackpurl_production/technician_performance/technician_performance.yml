version: 2


models:
  - name: technician_performance_model
    description: "Update technician_performance table with the latest records from technician_performance_stage"
    config:
      materialized: incremental
    columns:
      - name: TECHNICIAN
        description: "Technician name"
        tests:
      - name: DATE
        description: "Date"
        tests:

